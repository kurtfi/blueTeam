{
  "id": "agentix-brute-force-workflow-v3",
  "name": "Agentix Brute Force Response (T1110) v3",
  "description": "Automated SOAR playbook for MITRE T1110 (Brute Force). Flow: Wazuh alert → Forward to Agentix Webhook.",
  "tags": ["mitre", "t1110", "brute-force", "wazuh", "agentix"],
  "execution_variables": [],
  "workflow_variables": [
    {
      "name": "AGENTIX_WEBHOOK_URL",
      "value": "http://agentix-gateway:8001/v1/webhooks/shuffle/wazuh",
      "description": "Agentix core API webhook endpoint URL"
    },
    {
      "name": "AGENTIX_WEBHOOK_SECRET",
      "value": "dev-webhook-secret-key-change-me",
      "description": "HMAC secret key to sign outgoing webhook alerts"
    },
    {
      "name": "AGENTIX_INTERNAL_API_KEY",
      "value": "dev-internal-key-change-me-in-production",
      "description": "Agentix internal API key for authentication bypass"
    }
  ],
  "triggers": [
    {
      "id": "webhook_wazuh_brute_force",
      "name": "wazuh_brute_force",
      "type": "webhook",
      "status": "running",
      "workflows": ["agentix-brute-force-workflow-v3"],
      "start": "step_1_forward_to_agentix"
    }
  ],
  "actions": [
    {
      "app_name": "http",
      "app_version": "1.4.0",
      "app_id": "http",
      "name": "POST",
      "label": "Forward Alert to Agentix Webhook",
      "action": "POST",
      "environment": "Shuffle",
      "id": "step_1_forward_to_agentix",
      "isStartNode": true,
      "description": "Relays the alert directly to Agentix Gateway triage endpoint.",
      "parameters": [
        {
          "name": "url",
          "value": "$AGENTIX_WEBHOOK_URL"
        },
        {
          "name": "headers",
          "value": "Content-Type: application/json\nX-Internal-Api-Key: $AGENTIX_INTERNAL_API_KEY"
        },
        {
          "name": "body",
          "value": "$exec"
        }
      ],
      "next": []
    }
  ],
  "_comment": "IMPORT INSTRUCTIONS: Shuffle UI (http://localhost:3001) → Workflows → Import Workflow → select this file. Set workflow variables: AGENTIX_WEBHOOK_URL, AGENTIX_WEBHOOK_SECRET. Alternatively, run: uv run python scripts/setup_shuffle.py"
}
