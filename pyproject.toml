[tool.uv]
workspace = { members = ["src/Agentix", "src/TriageCore", "src/AgenticCommon", "src/AttackSimulator", "src/DatasetIngestor"] }

[dependency-groups]
dev = [
    "pytest-cov>=7.1.0",
]

[[tool.mypy.overrides]]
module = [
    "yaml.*",
    "langfuse.*",
    "asyncpg.*",
    "pgvector.*",
    "general_mcp.*"
]
ignore_missing_imports = true
